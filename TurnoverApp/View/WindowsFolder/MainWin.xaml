<Window x:Class="TurnoverApp.View.PagesFolder.MainWin"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TurnoverApp.View.PagesFolder"
        mc:Ignorable="d"
        Title="Товарооборот" Height="450" Width="1000">

    <Window.Resources>
        <Style x:Key="ButtonBar" TargetType="Button">
            <Setter Property="Height" Value="35"/>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <StackPanel>
            <Button Style="{StaticResource ButtonBar}" Content="Товары" />
            <Button Style="{StaticResource ButtonBar}" Content="Продажи"/>
            <Button Style="{StaticResource ButtonBar}" Content="Закупки"/>
            <Button Style="{StaticResource ButtonBar}" Content="Показатели"/>
            <Button Style="{StaticResource ButtonBar}" Content="Склад"/>
        </StackPanel>
        
    </Grid>
</Window>
